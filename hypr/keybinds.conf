# ============================================================
# 快捷键绑定
# 说明：本文件通过问答逐项建立；未确认的绑定请勿擅自添加。
# 文档：https://wiki.hyprland.org/Configuring/Binds/
# ============================================================

# 主修饰键：SUPER（Win 键）
$mainMod = SUPER

# ------------------------------------------------------------
# 1. 窗口与会话
# ------------------------------------------------------------

# 打开终端（foot）
bind = $mainMod, Return, exec, foot

# 关闭当前窗口
bind = $mainMod, Q, killactive,

# 强制杀窗（进入点选杀窗模式）
bind = $mainMod SHIFT, Q, exec, hyprctl kill

# 退出 Hyprland
bind = $mainMod SHIFT, E, exit,

# 切换浮动 / 平铺
bind = $mainMod, T, togglefloating,

# dwindle：切换当前容器横/竖分屏（需 preserve_split = true）
bind = $mainMod CTRL, Return, layoutmsg, togglesplit

# dwindle ↔ master 布局切换
bind = $mainMod ALT, Return, exec, $scriptsDir/toggle-layout.sh

# 伪全屏（最大化类，保留间隙/栏等行为依版本而定）
bind = $mainMod, M, fullscreen, 1

# 真全屏
bind = $mainMod SHIFT, M, fullscreen, 2

# 重载 Hyprland 配置
bind = $mainMod CTRL SHIFT, R, exec, hyprctl reload

# ------------------------------------------------------------
# 2. 焦点与移动
# ------------------------------------------------------------

# 移动焦点（HJKL）
bind = $mainMod, H, movefocus, l
bind = $mainMod, L, movefocus, r
bind = $mainMod, K, movefocus, u
bind = $mainMod, J, movefocus, d

# 移动窗口（HJKL）
bind = $mainMod SHIFT, H, movewindow, l
bind = $mainMod SHIFT, L, movewindow, r
bind = $mainMod SHIFT, K, movewindow, u
bind = $mainMod SHIFT, J, movewindow, d

# 调整窗口大小（按住重复；Super+Alt 与旧 hyprconfig 一致）
binde = $mainMod ALT, H, resizeactive, -50 0
binde = $mainMod ALT, L, resizeactive, 50 0
binde = $mainMod ALT, K, resizeactive, 0 -50
binde = $mainMod ALT, J, resizeactive, 0 50

# 鼠标：Super + 左键拖移 / 右键缩放
bindm = $mainMod, mouse:272, movewindow
bindm = $mainMod, mouse:273, resizewindow

# ------------------------------------------------------------
# 3. 工作区
# ------------------------------------------------------------

# 切换到工作区 1–10（0 表示 10）
bind = $mainMod, 1, workspace, 1
bind = $mainMod, 2, workspace, 2
bind = $mainMod, 3, workspace, 3
bind = $mainMod, 4, workspace, 4
bind = $mainMod, 5, workspace, 5
bind = $mainMod, 6, workspace, 6
bind = $mainMod, 7, workspace, 7
bind = $mainMod, 8, workspace, 8
bind = $mainMod, 9, workspace, 9
bind = $mainMod, 0, workspace, 10

# 将窗口带到目标工作区并跟随切换
bind = $mainMod CTRL, 1, movetoworkspace, 1
bind = $mainMod CTRL, 2, movetoworkspace, 2
bind = $mainMod CTRL, 3, movetoworkspace, 3
bind = $mainMod CTRL, 4, movetoworkspace, 4
bind = $mainMod CTRL, 5, movetoworkspace, 5
bind = $mainMod CTRL, 6, movetoworkspace, 6
bind = $mainMod CTRL, 7, movetoworkspace, 7
bind = $mainMod CTRL, 8, movetoworkspace, 8
bind = $mainMod CTRL, 9, movetoworkspace, 9
bind = $mainMod CTRL, 0, movetoworkspace, 10

# 将窗口静默移到目标工作区（不跟随）
bind = $mainMod SHIFT, 1, movetoworkspacesilent, 1
bind = $mainMod SHIFT, 2, movetoworkspacesilent, 2
bind = $mainMod SHIFT, 3, movetoworkspacesilent, 3
bind = $mainMod SHIFT, 4, movetoworkspacesilent, 4
bind = $mainMod SHIFT, 5, movetoworkspacesilent, 5
bind = $mainMod SHIFT, 6, movetoworkspacesilent, 6
bind = $mainMod SHIFT, 7, movetoworkspacesilent, 7
bind = $mainMod SHIFT, 8, movetoworkspacesilent, 8
bind = $mainMod SHIFT, 9, movetoworkspacesilent, 9
bind = $mainMod SHIFT, 0, movetoworkspacesilent, 10

# 相对切换：下一个 / 上一个已有工作区
bind = $mainMod, period, workspace, e+1
bind = $mainMod, comma, workspace, e-1

# 多显示器：在显示器之间切换工作区
bind = $mainMod, Tab, workspace, m+1
bind = $mainMod SHIFT, Tab, workspace, m-1

# --- 具名特殊工作区：BackSpace ---
# Super：切换显示；Shift：静默移入；Ctrl：移入并跟随
bind = $mainMod, BackSpace, togglespecialworkspace, backspace
bind = $mainMod SHIFT, BackSpace, movetoworkspacesilent, special:backspace
bind = $mainMod CTRL, BackSpace, movetoworkspace, special:backspace

# --- 具名特殊工作区：backslash ---
bind = $mainMod, backslash, togglespecialworkspace, backslash
bind = $mainMod SHIFT, backslash, movetoworkspacesilent, special:backslash
bind = $mainMod CTRL, backslash, movetoworkspace, special:backslash

# ------------------------------------------------------------
# 4. 媒体与笔记本功能键
# 状态：基础媒体键已接；键盘背光、触控板、飞行模式、合盖仍延后
# ------------------------------------------------------------

# 锁屏（hyprlock；空闲自动锁未启用）
# 仅 eDP-1 + 纯色背景（见 hyprlock.conf）；勿对 headless 截屏模糊。
bind = CTRL ALT, L, exec, hyprlock

# 休眠键 → 锁屏（与 Ctrl+Alt+L 相同）
bind = , XF86Sleep, exec, hyprlock

# 音量 / 麦克风（wpctl；步进 5%，与 Waybar 滚轮一致）
bind = , XF86AudioRaiseVolume, exec, $scriptsDir/volume.sh --inc
bind = , XF86AudioLowerVolume, exec, $scriptsDir/volume.sh --dec
bind = , XF86AudioMute, exec, $scriptsDir/volume.sh --toggle
bind = , XF86AudioMicMute, exec, $scriptsDir/volume.sh --toggle-mic

# 屏幕亮度（brightnessctl；步进 10%，与 Waybar 一致）
bind = , XF86MonBrightnessUp, exec, $scriptsDir/brightness.sh --inc
bind = , XF86MonBrightnessDown, exec, $scriptsDir/brightness.sh --dec

# 媒体播放（playerctl）
bind = , XF86AudioPlay, exec, playerctl play-pause
bind = , XF86AudioNext, exec, playerctl next
bind = , XF86AudioPrev, exec, playerctl previous
bind = , XF86AudioStop, exec, playerctl stop

# --- 延后（需问答确认后再加）---
# 键盘背光：XF86KbdBrightnessUp/Down → brightnessKbd.sh
# 触控板开关：XF86TouchpadToggle → 需 hyprctl devices 里的设备名
# 飞行模式：XF86Rfkill
# 合盖：switch:Lid Switch
# ASUS 专用：XF86Launch1/3/4 → rog-control-center / asusctl

# ------------------------------------------------------------
# 5. 启动器 / 壁纸 / 截图 / 剪贴板等
# ------------------------------------------------------------

# Wofi 应用启动器（再按一次关闭）
bind = $mainMod, Space, exec, ~/.config/wofi/launch.sh

# Wofi 运行命令
bind = $mainMod, R, exec, ~/.config/wofi/launch-run.sh

# 随机切换壁纸（awww）
bind = $mainMod, W, exec, $scriptsDir/wallpaper.sh random

# 截图（grim + swappy；P 键避免与 paste 冲突故用 P）
bind = $mainMod, P, exec, $scriptsDir/screenshot.sh --now
bind = $mainMod ALT, P, exec, $scriptsDir/screenshot.sh --win
bind = $mainMod SHIFT ALT, P, exec, $scriptsDir/screenshot.sh --area

# 剪贴板历史（cliphist + Wofi）
bind = $mainMod, V, exec, ~/.config/wofi/launch-clip.sh
