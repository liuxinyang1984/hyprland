# ============================================================
# 环境 overlay — 家用笔记本（main 默认）
# 办公室等环境请用 office 分支替换本文件（见 env/office.conf.example）
# ============================================================

exec-once = echo "Xft.dpi: 120" | xrdb -merge

monitor = eDP-1, preferred, 0x0, 1.25
monitor = headless, disable
monitor = , preferred, auto, 1.25
