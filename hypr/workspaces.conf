# ============================================================
# 工作区 — 公共策略（占位）
# 暂不写全局 workspace 绑定；各环境如需绑定 monitor，写在 env.conf。
# 例：workspace = 1, monitor:DP-1
# ============================================================
